apiVersion: v1
kind: Service
metadata:
  name: platform-demo
  labels:
    app: platform-demo
spec:
  type: LoadBalancer
  selector:
    app: platform-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP